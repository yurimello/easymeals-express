machine:
  pre:
    - sudo apt-get update
    - sudo apt-get install expect
test:
  override:
    - echo "test"
    
deployment:
  release:
    tag: /[0-9]+(\.[0-9]+)+/
    owner: suggestgrid
    commands:
      - ./.deploy
