extends ../layout

block content
  .w3-row-padding.w3-padding-16.w3-center#food
    for receipe in receipes
      .w3-quarter
        img(src="#{receipe.image}" alt="Sandwich") 
        h3= receipe.name
        p Just some random text, lorem ipsum text praesent tincidunt ipsum lipsum
  
  //- Pagination
  .w3-center.w3-padding-32
    ul.w3-pagination
      if page > 1
        li 
          a.w3-hover-black(href="/receipes?page=1") «
      - for(i = pageRange.firstPage; i <= pageRange.lastPage; i++)
        li
          if i == page 
            a.w3-black(href="#")= i
          else
            a.w3-hover-black(href="/receipes?page=#{i}")= i
      if page < pages
        li
          a.w3-hover-black(href="/receipes?page=#{pages}") »
        //- li 
        //-   
        //- li 
        //-   a.w3-hover-black(href="#") 3
        //- li 
        //-   a.w3-hover-black(href="#") 4
      
        
  hr#about

  //- About Section
  .w3-container.w3-padding-32.w3-center
    h3 Next Section
