#!/usr/bin/env node

const repl = require('repl');

// repl.start('> ');

var User = require('../models/user');
var Receipe = require('../models/receipe');

var cli = repl.start({prompt: '> '});

cli.context.User = User;
cli.context.Receipe = Receipe;
